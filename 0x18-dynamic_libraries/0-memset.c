#include "main.h"

/**
 * _memset - fills the memory with constant byte
 * @s: the pointer
 * @b: the buffer
 * @n: number of bytes
 *
 * Return: s.
 */
char *_memset(char *s, char b, unsigned int n)
{
}
