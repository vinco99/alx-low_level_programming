#include "main.h"

/**
 * _abs - calculate the absolue value of an int
 * @n: parameter an integer
 *
 * Return: the absolute value of n
 */
int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

}
