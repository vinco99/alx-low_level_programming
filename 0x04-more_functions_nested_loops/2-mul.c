#include "main.h"

/**
 * mul - Multiplies two numbers
 * @a: the first number
 * @b: the second number
 *
 * Return: result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
